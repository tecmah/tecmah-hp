name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
    # Remove pull_request trigger for production deployments

# Allow only one concurrent deployment
concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    # このジョブが GITHUB_TOKEN に求めるのは checkout の読み取りだけ。
    # 明示しないとリポジトリ既定を継承する（本コミット時点の既定は write）。
    # deploy ジョブの permissions は build には適用されないので、ここに書く必要がある。
    # upload-pages-artifact はアーティファクト用の実行時トークンを使うため contents: read で足りる
    # （GitHub 公式の Pages スターターワークフローも build に write を与えていない）。
    permissions:
      contents: read

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for git info
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build Astro site
      run: npm run build
      
    - name: Create CNAME file for custom domain
      run: echo "www.tecmah.com" > ./dist/CNAME
      
    - name: Upload Pages Artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    # Grant GITHUB_TOKEN the permissions required to make a Pages deployment
    permissions:
      pages: write      # to deploy to Pages
      id-token: write   # to verify the deployment originates from an appropriate source
      contents: read    # to read the repository contents
      
    # Deploy to the github-pages environment
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
      
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4